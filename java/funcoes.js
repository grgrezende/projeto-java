// function nomeDaFuncao(argumento1, argumento){
//  // qualquer coisa que a sua função precisa executar;
// return valorDeRetorno;    
//}

function raizQuadrada(lista([6, 10, 70])){
    let resultado = [];
    for (let numero of lista){
        resultados.push(Math.sqrt(numero));
    }
    return resultados;
}

function dizerOla(){
    return "ola";
}

function validarnumero(entrada){
    if(entrada === null || isNaN (Number(entrada))){
        return false;
    }
    return true;
    
}